<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
namespace="com.yorimichi.yorimichi.domain.admin.users.repository.AdminMemberMapper">
	<resultMap id="adminMemberResultMap"
	type="com.yorimichi.yorimichi.domain.admin.users.dto.AdminMemberResponseDto">
		<id property="memberId" column="member_id"/>
		<result property="email" column="email"/>
		<result property="name" column="name"/>
		<result property="phone" column="phone"/>
		<result property="role" column="role"/>
		<result property="status" column="status"/>
		<result property="withdrawnAt" column="withdrawn_at"/>
	</resultMap>
	
	<select id="findAllMembers" resultMap="adminMemberResultMap">
		SELECT
			member_id,
			email,
			name,
			phone,
			role,
			status,
			withdrawn_at
		FROM MEMBER
		ORDER BY created_at DESC, member_id DESC
	</select>
</mapper>