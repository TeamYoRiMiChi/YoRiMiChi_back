<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper 
namespace="com.yorimichi.yorimichi.domain.mypage.repository.MypageCartMapper">

	<resultMap id = "mypageCartResultMap"
	type="com.yorimichi.yorimichi.domain.mypage.entity.MypageCart">
	<id
		property="cartItemId"
		column="cart_item_id"
	/>
	<result property="cartId" column="cart_id" />
	<result property="memberId" column="member_id" />
	<result property="productId" column="product_id" />
	<result property="quantity" column="quantity" />
	<result property="createdAt" column="created_at" />
	<result property="updatedAt" column="updated_at" />
	
	<result property="productName" column="product_name" />
	<result property="priceJpy" column="price_jpy" />
	<result property="thumbnailUrl" column="thumbnail_url" />
	<result property="stock" column="stock" />
	<result property="productStatus" column="product_status" />
</resultMap>

	<select
		id="findAllByMemberId"
		resultMap="mypageCartResultMap"
	>
		SELECT
			ci.cart_item_id,
			ci.cart_id,
			c.member_id,
			ci.product_id,
			ci.quantity,
			ci.created_at,
			ci.updated_at,
			COALESCE(
				p.product_name_jp,
				p.product_name
			) AS product_name,
			p.price_jpy,
			p.thumbnail_url,
			p.stock,
			p.status AS product_status
		FROM CART c
		INNER JOIN CART_ITEM ci
			ON ci.cart_id = c.cart_id
		INNER JOIN PRODUCT p
			ON p.product_id = ci.product_id
		WHERE c.member_id = #{memberId}
		ORDER BY
			ci.created_at DESC,
			ci.cart_item_id DESC
			
	</select>
	
</mapper>